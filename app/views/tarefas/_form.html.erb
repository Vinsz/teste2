<%= form_for(tarefa) do |f| %>
  <% if tarefa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tarefa.errors.count, "error") %> prohibited this tarefa from being saved:</h2>

      <ul>
      <% tarefa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :listum_id %>
    <%= f.text_field :listum_id %>
  </div>

  <div class="field">
    <%= f.label :texto %>
    <%= f.text_field :texto %>
  </div>

  <div class="field">
    <%= f.label :aberta %>
    <%= f.check_box :aberta %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
